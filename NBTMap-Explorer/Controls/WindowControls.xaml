<UserControl x:Class="NBTMap_Explorer.Controls.WindowControls"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             Height="40"
             UseLayoutRounding="True"
             SnapsToDevicePixels="True">

    <!-- VM de la Window -->
    <UserControl.DataContext>
        <Binding RelativeSource="{RelativeSource AncestorType=Window}"
                 Path="DataContext" />
    </UserControl.DataContext>

    <!-- HEADER COMPLETO = ZONA DRAG -->
    <Border x:Name="Controls_Container"
            Height="40"
            Background="Transparent"
            VerticalAlignment="Stretch"
            HorizontalAlignment="Stretch"
            WindowChrome.IsHitTestVisibleInChrome="True">

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- IZQUIERDA: icono + título (drag permitido) -->
            <StackPanel Grid.Column="0"
                        Orientation="Horizontal"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        WindowChrome.IsHitTestVisibleInChrome="True">

                <Image Height="24"
                       Source="/Assets/Icons/app-icon.ico"
                       IsHitTestVisible="False"/>

                <TextBlock Text="{Binding Title}"
                           VerticalAlignment="Center"
                           Margin="10,0,0,0"
                           FontSize="14"
                           FontWeight="SemiBold"
                           IsHitTestVisible="False"/>
            </StackPanel>

            <!-- DERECHA: botones (NO drag) -->
            <StackPanel Grid.Column="1"
                        Orientation="Horizontal"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Right"
                        Margin="0,0,10,0"
                        Height="40"
                        WindowChrome.IsHitTestVisibleInChrome="False">

                <Button x:Name="Btn_Minimize"
                        Content=""
                        Style="{StaticResource WindowButton}"
                        ToolTip="Minimizar"
                        Command="{Binding MinimizeCommand}"
                        Cursor="Hand"
                        WindowChrome.IsHitTestVisibleInChrome="True"/>

                <Button x:Name="Btn_Maximize"
                        Content="{Binding MaxRestoreIcon}"
                        Style="{StaticResource WindowButton}"
                        ToolTip="Maximizar / Restaurar"
                        Command="{Binding MaximizeCommand}"
                        Cursor="Hand"
                        WindowChrome.IsHitTestVisibleInChrome="True"/>

                <Button x:Name="Btn_Close"
                        Content=""
                        Style="{StaticResource CloseButton}"
                        ToolTip="Cerrar"
                        Command="{Binding CloseCommand}"
                        Cursor="Hand"
                        WindowChrome.IsHitTestVisibleInChrome="True"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
