<local:BaseWindow x:Class="NBTMap_Explorer.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NBTMap_Explorer.Views"
        xmlns:vm="clr-namespace:NBTMap_Explorer.ViewModels"
        xmlns:c="clr-namespace:NBTMap_Explorer.Controls"
        mc:Ignorable="d"
        Title="MainWindow" 
        Height="600" 
        Width="800"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="CanResize"
        Cursor=""
        AllowsTransparency="False">
    <Window.DataContext>
        <vm:MainWindowViewModel />
    </Window.DataContext>

    <WindowChrome.WindowChrome>
        <WindowChrome
        CaptionHeight="40"
        ResizeBorderThickness="8"
        CornerRadius="10"
        GlassFrameThickness="0"
        UseAeroCaptionButtons="True" />
    </WindowChrome.WindowChrome>

    <c:ApplicationLayout>
        <TextBlock Text="Hola mundo prueba" />
    </c:ApplicationLayout>
</local:BaseWindow>
